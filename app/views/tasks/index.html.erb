
<% @tasks.each do |task| %>
  <h5> <%= link_to task.title, task_path(task) %></h5>
  <p> <%= task.details %></p>
  <p> <%= task.completed %></p>
  <%= link_to "Edit", edit_tasks_path(task) %>
  <p></p>
<% end %>
